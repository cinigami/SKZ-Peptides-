[build]
  # Directory with the finished build
  publish = "dist"
  
  # Build command
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"